<IfModule mod_rewrite.c>
    RewriteEngine On

    # Ensure requests are not for existing files or directories
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Force everything through public/index.php
    RewriteRule ^(.*)$ public/index.php [L]
</IfModule>
